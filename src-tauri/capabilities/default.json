{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Neattttty default capability (core only; scenes persist via localStorage + file export in v1)",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
